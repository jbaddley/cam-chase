{
  "name": "@photochase/infra",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "synth": "cdk synth",
    "lint": "eslint bin lib"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.174.0",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "aws-cdk": "^2.174.0",
    "esbuild": "^0.28.1",
    "tsx": "^4.19.2"
  }
}
